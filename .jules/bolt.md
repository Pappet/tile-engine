## 2024-05-26 - Optimizing periodic_reaction_system loops
**Learning:** The nested loops in periodic_reaction_system iterate over every chunk, then every periodic reaction, then every index in the chunk. O(Chunks * PeriodicReactions * 1024). This is very expensive and we can swap the loop order and pre-filter. By pre-fetching active periodic reactions and only checking relevant tiles, we can optimize. Wait, memory says: 'Optimizing simulation systems (like `periodic_reaction_system`) by pre-filtering and pre-fetching active registry entries into a `Vec` outside of hot chunk/tile loops significantly reduces redundant work from O(Chunks * TotalReactions) to O(TotalReactions + Chunks * ActiveReactions).' I should implement this.
